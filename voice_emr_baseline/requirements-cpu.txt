# Voice EMR Baseline — CPU 전용 의존성 (GPU 없을 때)
# LLM SOAP 요약은 매우 느리므로 --skip-llm 권장

faster-whisper>=1.0.3
keybert>=0.8.4
sentence-transformers>=2.7.0
scikit-learn>=1.3.0

# CPU 전용 PyTorch (작아짐)
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
transformers>=4.45.0
accelerate>=0.34.0

# CPU 최적화 LLM 백엔드 (GGUF) — 5~10배 빠름
llama-cpp-python>=0.2.90

gradio>=4.44.0
pandas>=2.0.0
psutil>=5.9.0
soundfile>=0.12.0
